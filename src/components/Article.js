import React from 'react';

const Article = (props) => {
    console.log(props)

    return (

        <div>
        {/* {props.headlines.map(i => {
            return <p>{i.title}</p>
            }

        )} */}
        </div>
    )
}

export default Article